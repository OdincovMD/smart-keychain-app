import 'package:flutter/widgets.dart';

/// Theme-independent geometry shared by Chrome Kiss fidelity screens.
abstract final class ChromeKissFidelityTokens {
  static const referenceSize = Size(393, 852);
  static const screenRadius = 48.0;
  static const compactScreenRadius = 36.0;
  static const contentInset = 24.0;
  static const panelRadius = 32.0;
}
